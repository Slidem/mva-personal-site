export interface BlogPostMetadata {
  title: string;
  shortTitle: string;
  description: string;
  shortDescription: string;
  publishDate: string;
  url?: string;
  canonicalUrl: string;
  tags: string[];
}
