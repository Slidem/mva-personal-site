import Layout from "@/components/blog/BlogPostLayout";

export const meta = {
  title: "How to denormalize data in DynamoDB for analytics",
  shortTitle: "DynamoDB Data Denormalization",
  description: "DynamoDB is a great NoSQL database. It is fast, reliable and serverless. But it has some limitations. One of them is that it's not good at analytics. In this article we will look at how to denormalize data in DynamoDB to make it more suitable for analytics...",
  shortDescription: "Analytics and Data denormalization in DanamoDB",
  publishDate: "07/28/2023",
  canonicalUrl: "https://alextechspace.com/blog/denormalizing-data-in-dynamodb",
  tags: ["dynamodb", "nosql"],
};

# Denormalizing data in DynamoDB

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;